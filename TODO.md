# TODO / Roadmap

Ниже список идей, которые подготовят проект к модели, где Telegram-бот и код остаются под моим контролем, а пользователи работают через веб-кабинет: регистрируются, настраивают свои сценарии, добавляют услуги, специалистов и свои цены. Также здесь есть задел под BPMN-сценарии.

## Архитектура и расширяемость

- Вынести текущий сценарий бронирования в модуль `src/scenarios/booking/*` и сделать диспетчер сценариев в webhook-роуте/HTTP API (например, по типу сценария, по настройкам аккаунта в БД, по опубликованному каналу записи).
- Определить интерфейс сценария: `handleMessage(...)`, `handleCallback(...)`, `getMainMenu(...)`, `getI18nNamespace(...)` и т.д.
- Добавить сущность аккаунта/рабочего пространства (`accounts` или `workspaces`) и связать с ней `users/services/specialists/appointments/app_settings/user_sessions`, чтобы один backend обслуживал много пользовательских кабинетов с изоляцией данных.
- Разделить роли: владелец системы, пользователь веб-кабинета, конечный клиент на записи; явно определить, кто чем управляет и какие данные видит.
- Ввести административный слой управления данными: HTTP API с auth для регистрации, входа, CRUD по `services/specialists/app_settings/scenarios`, а CLI (`npm run admin:*`) оставить только для системных операций владельца.
- Добавить веб-приложение/личный кабинет, где пользователь может зарегистрироваться, настроить сценарии, добавить услуги, специалистов, цены, расписание и другие параметры без прямого доступа к коду или БД.

## BPMN-сценарии

- Решить, что именно BPMN описывает: диалоговый граф, бизнес-процесс бронирования, либо несколько процессов (например, «запись», «перенос», «отмена», «оплата»).
- Завести папку `bpmn/` (или `docs/bpmn/`) с диаграммами и соглашением по аннотациям/props.
- Интегрировать [src/utils/BPMN/BPMN.ts](./src/utils/BPMN/BPMN.ts) как опциональный движок сценариев:
  - добавить зависимости `xpath`, `xmldom` (и типы при необходимости),
  - добавить слой runtime-состояния (токен процесса) в БД,
  - сопоставление BPMN task -> шаг бота (рендер текста/клавиатуры, валидация ввода).
- Добавить генерацию документации по BPMN: экспорт списка шагов/переходов в Markdown для README/PROJECT_MAP.

## Расписание, время, слоты

- [x] Перейти с фиксированного UTC+3 на IANA timezone из `app_settings.timezone` и убрать хардкод в `src/utils/timezone.ts`.
- Сделать шаг сетки слотов (`SLOT_STEP_MIN`) настраиваемым через `app_settings`.
- Добавить исключения расписания: праздники, выходные, «перерывы», индивидуальные окна для специалиста.
- [x] Добавить предотвращение гонок: DB-защита от пересечения интервалов записи (exclusion constraint по `account_id + specialist_id`).

## Уведомления и фоновые задачи

- [x] Реализован базовый контур `notifications`:
  - постановка напоминаний при создании записи,
  - фоновая джоба (`src/jobs/reminder.job.ts`) для отправки,
  - ретраи и статусная модель (`pending/retry/sent/failed/cancelled`).
- [x] Добавлены каналы Telegram + email + SMS на уровне БД и сервиса (email/SMS пока stub до подключения внешних провайдеров).
- [x] Доработать перенос/отмену: при переносе напоминания пересоздаются, при отмене помечаются `cancelled`.
- [x] Поддержана пакетная запись: количество сессий берется из `services.sessions_count`, создается серия записей с недельным шагом.
- [x] Для пакетной записи (`sessions_count > 1`) добавлен пошаговый выбор отдельных даты/времени для каждой сессии в боте.
- [x] Для пакетной записи добавлен быстрый режим: после первого слота можно автоматически создать оставшиеся еженедельные сессии в то же время.
- [x] Добавлена сущность `appointment_groups` для хранения общей стоимости/статуса оплаты пакета и связки с `appointments`.
- [x] Добавлены базовые ставки специалиста (`specialists.base_session_price`, `specialists.base_hour_price`) и расчет итоговой стоимости услуги от ставки специалиста.
- [ ] Подключить реальные email/SMS провайдеры (SMTP/API) и шаблоны сообщений.

## UX сценариев

- [x] Добавить отмену записи и статус `cancelled`.
- [x] Добавить базовый «назад» по шагам и явную команду сброса сессии (`/start`, `/reset`).
- Добавить «назад» на остальные шаги (контакты/подтверждение), чтобы можно было откатываться без рестарта сценария.
- Добавить «быстрый повтор» последней услуги/специалиста.
- Добавить подтверждение контактов и согласие на обработку данных (если нужно).

## DevOps и DX

- Добавить `docker-compose.yml` (Postgres + app), `README` для деплоя.
- Добавить `npm run typecheck` (tsc --noEmit) и включить его в CI.
- Добавить `AUTO_SET_WEBHOOK=0/1` или `SET_WEBHOOK_ON_START=...`, чтобы локально не трогать webhook.
- Добавить структурированные логи (request id, update id) и метрики (health details).
